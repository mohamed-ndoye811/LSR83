import './Equipe.svelte.css.proxy.js';
/* src\pages\Equipe.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	destroy_each,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space,
	text
} from "../../_snowpack/pkg/svelte/internal.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[1] = list[i];
	return child_ctx;
}

// (24:2) {#each equipe as pannel}
function create_each_block(ctx) {
	let div1;
	let p;
	let t0_value = /*pannel*/ ctx[1].titre + "";
	let t0;
	let t1;
	let div0;
	let t2_value = (/*pannel*/ ctx[1].ouvert == false ? "+" : "-") + "";
	let t2;
	let t3;

	return {
		c() {
			div1 = element("div");
			p = element("p");
			t0 = text(t0_value);
			t1 = space();
			div0 = element("div");
			t2 = text(t2_value);
			t3 = space();
			attr(p, "class", "option__titre");
			attr(div0, "class", "option__ouvert");
			attr(div1, "class", "option svelte-1unlzyn");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, p);
			append(p, t0);
			append(div1, t1);
			append(div1, div0);
			append(div0, t2);
			append(div1, t3);
		},
		p: noop,
		d(detaching) {
			if (detaching) detach(div1);
		}
	};
}

function create_fragment(ctx) {
	let div;
	let h1;
	let t1;
	let each_value = /*equipe*/ ctx[0];
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	return {
		c() {
			div = element("div");
			h1 = element("h1");
			h1.textContent = "ÉQUIPE";
			t1 = space();

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			attr(h1, "id", "titrePage");
			attr(h1, "class", "svelte-1unlzyn");
			attr(div, "class", "container svelte-1unlzyn");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, h1);
			append(div, t1);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(div, null);
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*equipe*/ 1) {
				each_value = /*equipe*/ ctx[0];
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(div, null);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}

				each_blocks.length = each_value.length;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			destroy_each(each_blocks, detaching);
		}
	};
}

function instance($$self) {
	let equipe = [
		{
			titre: "Conseil d'administration",
			ouvert: false
		},
		{
			titre: "Commission de contrôle",
			ouvert: false
		},
		{
			titre: "Responsables d'Actvité",
			ouvert: false
		},
		{ titre: "Les antennes", ouvert: false }
	];

	return [equipe];
}

class Equipe extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Equipe;