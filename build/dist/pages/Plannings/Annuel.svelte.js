import './Annuel.svelte.css.proxy.js';
/* src\pages\Plannings\Annuel.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	destroy_each,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "../../../_snowpack/pkg/svelte/internal.js";

import { each } from "../../../_snowpack/pkg/svelte/internal.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[0] = list[i];
	return child_ctx;
}

// (31:6) {#each Array(20) as _}
function create_each_block(ctx) {
	let li;

	return {
		c() {
			li = element("li");

			li.innerHTML = `<p>10-17 OCTOBRE</p> 
          <p>SORTIE À EVIAN</p> 
          <p>ANTENNE</p> 
        `;

			attr(li, "class", "svelte-ugy6hc");
		},
		m(target, anchor) {
			insert(target, li, anchor);
		},
		d(detaching) {
			if (detaching) detach(li);
		}
	};
}

function create_fragment(ctx) {
	let div2;
	let div0;
	let t8;
	let div1;
	let ul;
	let each_value = Array(20);
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	return {
		c() {
			div2 = element("div");
			div0 = element("div");

			div0.innerHTML = `Pour toutes sorties en car les participants doivent verser un acompte, pour
    frais de dossier, de 20€ à l&#39;inscription.<br/><br/>

    Cet acompte, qui correspond au coût du transport, ainsi que les 10€
    d’assurance ne seront pas remboursés si l’annulation se fait moins de 45
    jours avant le départ.<br/><br/>

    Les personnes participant aux voyages et non adhérents (famille, amis)
    doivent signer une decharge<br/><br/>

    Pour des raisons de commodités il n&#39;est plus possible d&#39;attendre les
    participants au delà de 5 minutes.<br/><br/>

    Notre association reçoit, de la mairie de Toulon, quelques invitations pour
    des spectacles à l&#39;opéra vu le nombre restreint de places (5 !) attribuées
    il sera établi un roulement.<br/><br/> 

    <b><u>UNIQUEMENT POUR LES TOULONNAIS :</u></b><br/>
    Il est possible d&#39;obtenir des places individuellement en se présentant 15 jours
    avant la date au Service Animation Séniors, 10 place louis Blanc`;

			t8 = space();
			div1 = element("div");
			ul = element("ul");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			attr(div0, "class", "infoPlanningAnnuel svelte-ugy6hc");
			attr(ul, "class", "activites svelte-ugy6hc");
			attr(div1, "class", "activitesAnnee svelte-ugy6hc");
			attr(div2, "class", "container svelte-ugy6hc");
		},
		m(target, anchor) {
			insert(target, div2, anchor);
			append(div2, div0);
			append(div2, t8);
			append(div2, div1);
			append(div1, ul);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(ul, null);
			}
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div2);
			destroy_each(each_blocks, detaching);
		}
	};
}

class Annuel extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Annuel;