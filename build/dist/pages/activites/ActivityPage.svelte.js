/* src\pages\activites\ActivityPage.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../../../_snowpack/pkg/svelte/internal.js";

function create_fragment(ctx) {
	let h1;
	let t;

	return {
		c() {
			h1 = element("h1");
			t = text(/*activite*/ ctx[0]);
		},
		m(target, anchor) {
			insert(target, h1, anchor);
			append(h1, t);
		},
		p(ctx, [dirty]) {
			if (dirty & /*activite*/ 1) set_data(t, /*activite*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h1);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { activite } = $$props;

	$$self.$$set = $$props => {
		if ("activite" in $$props) $$invalidate(0, activite = $$props.activite);
	};

	return [activite];
}

class ActivityPage extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { activite: 0 });
	}
}

export default ActivityPage;