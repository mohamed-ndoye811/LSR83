import './CompteurVisite.svelte.css.proxy.js';
/* src\components\CompteurVisite.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	binding_callbacks,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../../_snowpack/pkg/svelte/internal.js";

import { gsap, Expo } from "../../_snowpack/pkg/gsap.js";
import { onMount, onDestroy } from "../../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let div;
	let p0;
	let t0;
	let t1;
	let p1;

	return {
		c() {
			div = element("div");
			p0 = element("p");
			t0 = text(/*compteurVisites*/ ctx[1]);
			t1 = text("\r\n   \r\n  ");
			p1 = element("p");
			p1.textContent = "VISITES";
			attr(p0, "class", "visitCount__nombre");
			attr(div, "class", "visitCount svelte-yxc8l");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, p0);
			append(p0, t0);
			append(div, t1);
			append(div, p1);
			/*div_binding*/ ctx[2](div);
		},
		p(ctx, [dirty]) {
			if (dirty & /*compteurVisites*/ 2) set_data(t0, /*compteurVisites*/ ctx[1]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			/*div_binding*/ ctx[2](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let compteur;

	onMount(() => {
		gsap.from(compteur, {
			y: 500,
			duration: 1,
			ease: Expo.easeOut,
			stagger: 0.1
		});
	});

	//---[ COMPTEUR DE VISITES ]---
	/*
INFOS POUR LE COMPTEUR:

Namespace: LSR83000VisitCount
clé: nombreVisites

Lien site pour voir son état | https://api.countapi.xyz/get/LSR83000VisitCount/nombreVisites
Lien pour le remettre à zéro | https://api.countapi.xyz/set/LSR83000VisitCount/nombreVisites?value=0
*/
	let compteurVisites = "";

	updateVisitCount();

	function updateVisitCount() {
		fetch("https://api.countapi.xyz/update/LSR83000VisitCount/nombreVisites?amount=1").then(res => res.json()).then(res => {
			$$invalidate(1, compteurVisites = res.value);
		});
	}

	function div_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			compteur = $$value;
			$$invalidate(0, compteur);
		});
	}

	return [compteur, compteurVisites, div_binding];
}

class CompteurVisite extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default CompteurVisite;